# 设计规范（Design System）

这份文档是本站点的**唯一设计依据**。所有颜色、字号、圆角、阴影、组件
样式都以 `src/styles/global.css` 顶部的 CSS 变量为准，改样式只改这里，
组件里不要写死色值 / 圆角 / 字号。

## 设计原则

- **现代极简**：留白优先，一个页面只保留一个视觉焦点
- **编辑式排版**：小标签（序号/类别）+ 大标题，形成信息层级
- **墨色 + 陶土橙**：文字用墨色，强调用陶土橙，克制使用彩色
- **自动深色模式**：跟随系统，变量在 `@media (prefers-color-scheme: dark)` 中整体覆盖

## 色彩 Token

| 变量 | 亮色模式 | 深色模式 | 用途 |
| --- | --- | --- | --- |
| `--bg` | `#fafaf7` | `#121211` | 页面背景 |
| `--surface` | `#ffffff` | `#1a1a19` | 卡片 / 浮层表面 |
| `--surface-muted` | `#f2f2ec` | `#232322` | 弱表面（占位、标签底） |
| `--text` | `#1a1a18` | `#f2f2ed` | 主文字 / 主按钮底 |
| `--text-secondary` | `#6e6e68` | `#a8a8a1` | 次级文字 |
| `--text-tertiary` | `#6d6d62` | `#8a8a80` | 弱文字/提示（12–13px 下 ≥4.5:1） |
| `--accent` | `#b44720` | `#e0764a` | 强调色（陶土橘，13px 小字 ≥4.5:1） |
| `--accent-strong` | `#a34321` | `#b8502e` | 强调色深：主按钮 hover |
| `--accent-soft` | `#f7e9e2` | `#33231b` | 强调色浅底（名字高亮等） |
| `--border` | `#e7e7e1` | `#2a2a28` | 分隔线 / 边框 |

> 语义：`--accent-strong` 在两种模式下都比 `--accent` 更深，用于 hover/按压，
> 保证主按钮 hover 时白字对比度 ≥ 4.5:1。
>
> 实测（WCAG 2.1）：`--accent #b44720` 对 `--bg` 达 5.20:1，`--text-tertiary #6d6d62` 对 `--bg` 达 5.00:1，
> 均 ≥ 4.5:1，符合 AA。在深色模式下 `e0764a`/`8a8a80` 同样达标，无需调整。

## 字号阶梯

| 变量 | 值 | 用途 |
| --- | --- | --- |
| `--text-xs` | `12px` | 标签、时间、元信息 |
| `--text-sm` | `13px` | 说明性小字（页脚、标注） |
| `--text-md` | `15px` | 次级正文（导航、卡片描述、技能胶囊） |
| 正文 | `16px` | `body` 默认字号 |

大标题使用 `clamp()` 流式缩放（首页/分区/详情页标题），不固定死数值。

## 圆角 / 阴影 / 动效 Token

| 类别 | Token | 值 | 用途 |
| --- | --- | --- | --- |
| 圆角 | `--radius-sm` | `6px` | 行内代码、小圆角元素 |
| 圆角 | `--radius-md` | `10px` | 常规小卡片、logo 标 |
| 圆角 | `--radius-lg` | `18px` | 作品封面、占位图 |
| 圆角 | `--radius-xl` | `24px` | 联系大面板 |
| 圆角 | `--radius-pill` | `999px` | 胶囊按钮、标签 |
| 阴影 | `--shadow-sm` | `0 4px 18px rgb(0 0 0 / .10)` | 悬浮元素（封面箭头） |
| 阴影 | `--shadow-md` | `0 16px 32px rgb(0 0 0 / .08)` | 下拉面板 |
| 动效 | `--dur` / `--dur-slow` | `0.28s` / `0.6s` | 交互动效 / 图片缩放 |
| 缓动 | `--ease` | `cubic-bezier(.33, 1, .68, 1)` | 全局统一缓动 |

阴影在深色模式会自动加深（变量覆盖）。

## 组件库（全局类）

以下类在 `src/styles/global.css` 中全局维护，组件直接复用，**不要**在组件
`<style>` 里复制一份样式。

| 类 | 说明 |
| --- | --- |
| `.container` / `.section` | 页面容器与区块垂直留白 |
| `.section-head` + `.section-title` | 分区标题（序号标签 + 大标题） |
| `.eyebrow` | 小节标签：首屏头衔 / 分区序号 / 侧栏小标题 |
| `.tag` / `.tag-year` | 小标签（类别 / 年份，`tag-year` 为描边变体） |
| `.btn` / `.btn-primary` / `.btn-outline` | 按钮：实心 / 描边，胶囊形 |
| `.text-link` | 文字链接（带下划线出现动画） |
| `.fade-up` | 首屏淡入上移动画（`.fade-up-1..4` 控制延迟） |

## 响应式断点（唯一依据）

只允许这三档，改断点先改 `global.css` 顶部注释，再同步组件：

- **640px**：手机 — 汉堡菜单、单列内容
- **768px**：平板 — 作品网格切单列
- **1024px**：屏幕较宽 / 平板横屏 — 关于区切单列

## 汉堡菜单规范（Header）

移动端（≤640px）汉堡按钮与左侧 logo 遵循**视觉同源**原则：两个元素用同一套
容器语言（尺寸 / 圆角 / 边框），只在"图形"上表达差异。

| 维度 | 规范 | 依据 |
| --- | --- | --- |
| 视觉尺寸 | 32×32，与 `.logo-mark` 一致 | 视觉对称，同一容器语言 |
| 圆角 | `--radius-md`（10px） | 与 logo-mark 相同 |
| 边框 | `1px solid var(--border)`，hover 变 `--accent` | 复用描边语言（同 `.tag-year`） |
| 触控热区 | `::before { inset: -6px }` 扩到 44×44 | WCAG 2.2 AA 建议 ≥44px，热区膨胀不影响视觉 |
| 线条 | 3 条 16×2px，`--radius-pill` 圆角，`gap: 5px` | 16px 与 logo 内 14px 汉字视觉宽度匹配 |
| 激活态 | 墨色底 `--text` + 白色 X 线（`--bg`） | 与 logo-mark（墨底）形成配对印章感 |
| 展开动画 | 上线 `translateY(7px) rotate(45deg)`，下线对称，中线淡出 | 7px = gap 5px + 线高 2px 的中心距，精确重合 |
| 布局 | `inline-flex column`，`justify-content: center`，固定 gap | 不用 space-around，避免不同容器高度下位移失准 |
| 无障碍 | `aria-expanded` / `aria-controls`，Escape 关闭，点外部关闭 | 见"无障碍约定" |

> 原则：**视觉尺寸与 logo 对齐，触控尺寸对人手负责**。永远不要为加大热区
> 而放大视觉元素——用 `::before` 伪元素扩展即可。

## 无障碍约定

- 保留语义化标签（`nav` / `main` / `h1-6` 等），移动端菜单有 `aria-expanded`
- 所有可交互元素有统一 `:focus-visible` 焦点环（`--accent` 2px）
- 支持 `prefers-reduced-motion`，关闭滚动平滑与动画
- 文字层级保证最小对比度：正文 ≥ 4.5:1，大号文字 ≥ 3:1
- 触控目标：桌面 `.nav-cta`、详情页 `.back-link`、页脚 `.back-top`、英语区
  链接等小文本交互应给 `min-height` ≥ 40px 或足够 `padding`，以满足 WCAG 2.2
  AA（≥24px，但建议 ≥44px）
- 作品卡片箭头仅在 `:hover` 时显影：对触屏 / 键盘用户不可见；应在
  `@media (hover: none)` 时常显轻提示，或用 `:focus-within` 配合
  `:focus-visible` 同步显示，以免隐藏可点击信号

## 修改入口速查

- 改任何颜色 / 字号 / 圆角 / 阴影 / 动效 → `src/styles/global.css` 顶部变量
- 改按钮 / 标签 / 小节标签样式 → `global.css` 组件库段落
- 新增组件 → 优先复用上面的全局类，组内只写"自己的差异"
